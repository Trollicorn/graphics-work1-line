#include <stdio.h>
#include <stdlib.h>

void graphify(struct pic graph){
	graph->color = calloc(sizeof(struct rgb),500*500);
}
 
int plot(struct pic graph, int x, int y, int r, int g, int b);

int line(int x0,int y0,int x1,int y1){
	
	
}
